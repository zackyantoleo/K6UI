{
  "name": "k6ui",
  "version": "1.0.0",
  "description": "UI for building and running k6 load tests without writing code",
  "main": "server/index.js",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test",
    "measure:baseline": "node scripts/measure-baseline.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
